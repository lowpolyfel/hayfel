const canvas = document.getElementById('wave-scene');
const ctx = canvas.getContext('2d');
const COLORS=['#590209','#F20530','#A60321','#03588C','#025373'];
const CONFIG={sampleStep:24,speed:0.00032,blurBoost:18};
let w=0,h=0,dpr=1,layers=[];
const hexToRgb=(hex)=>{const c=hex.replace('#','');return {r:parseInt(c.slice(0,2),16),g:parseInt(c.slice(2,4),16),b:parseInt(c.slice(4,6),16)}};
const rgba=(hex,a)=>{const c=hexToRgb(hex);return `rgba(${c.r}, ${c.g}, ${c.b}, ${a})`;};
function createLayers(){layers=[{color:COLORS[0],y:.22,thickness:.22,amplitude:44,amplitude2:22,amplitude3:18,freq:.006,freq2:.010,freq3:.014,speed:.90,alpha:.18,blur:36,offset:0,direction:1},{color:COLORS[1],y:.36,thickness:.25,amplitude:52,amplitude2:26,amplitude3:18,freq:.0055,freq2:.009,freq3:.015,speed:.75,alpha:.16,blur:42,offset:1.8,direction:1},{color:COLORS[2],y:.50,thickness:.24,amplitude:50,amplitude2:28,amplitude3:16,freq:.0065,freq2:.011,freq3:.016,speed:.62,alpha:.14,blur:40,offset:3,direction:1},{color:COLORS[3],y:.62,thickness:.24,amplitude:56,amplitude2:20,amplitude3:22,freq:.005,freq2:.0105,freq3:.013,speed:.70,alpha:.16,blur:44,offset:2.2,direction:-1},{color:COLORS[4],y:.74,thickness:.26,amplitude:58,amplitude2:24,amplitude3:19,freq:.0052,freq2:.0095,freq3:.0145,speed:.58,alpha:.15,blur:46,offset:4.5,direction:-1},{color:COLORS[1],y:.48,thickness:.18,amplitude:38,amplitude2:16,amplitude3:12,freq:.008,freq2:.015,freq3:.019,speed:1.1,alpha:.10,blur:28,offset:5.2,direction:1}]}
function resize(){dpr=Math.min(window.devicePixelRatio||1,2);w=window.innerWidth;h=window.innerHeight;canvas.width=Math.floor(w*dpr);canvas.height=Math.floor(h*dpr);canvas.style.width=w+'px';canvas.style.height=h+'px';ctx.setTransform(dpr,0,0,dpr,0,0);createLayers()}
function waveValue(x,t,l){const d=l.direction;return Math.sin(x*l.freq+t*l.speed*d+l.offset)*l.amplitude+Math.sin(x*l.freq2-t*(l.speed*.72)*d+l.offset*1.7)*l.amplitude2+Math.sin(x*l.freq3+t*(l.speed*1.1)*d+l.offset*.6)*l.amplitude3}
function drawBackground(){ctx.clearRect(0,0,w,h);const bg=ctx.createRadialGradient(w*.5,h*.5,0,w*.5,h*.5,Math.max(w,h)*.85);bg.addColorStop(0,'rgba(12,18,30,.25)');bg.addColorStop(.52,'rgba(4,8,16,.62)');bg.addColorStop(1,'rgba(0,0,0,.95)');ctx.fillStyle=bg;ctx.fillRect(0,0,w,h)}
function drawWaveLayer(l,t){const yb=h*l.y,th=h*l.thickness,st=CONFIG.sampleStep;ctx.save();ctx.globalCompositeOperation='screen';ctx.shadowBlur=l.blur+CONFIG.blurBoost;ctx.shadowColor=rgba(l.color,.26);const grad=ctx.createLinearGradient(0,yb-th,0,yb+th*1.2);grad.addColorStop(0,rgba(l.color,0));grad.addColorStop(.18,rgba(l.color,l.alpha*.65));grad.addColorStop(.50,rgba(l.color,l.alpha));grad.addColorStop(.82,rgba(l.color,l.alpha*.58));grad.addColorStop(1,rgba(l.color,0));ctx.beginPath();let x=-st*2,y=yb+waveValue(x,t,l);ctx.moveTo(x,y);for(x=-st*2;x<=w+st*2;x+=st){y=yb+waveValue(x,t,l);ctx.lineTo(x,y)}for(x=w+st*2;x>=-st*2;x-=st){y=yb+th+waveValue(x,t,l)*.55;ctx.lineTo(x,y)}ctx.closePath();ctx.fillStyle=grad;ctx.fill();ctx.restore()}
function drawFloatingGlow(t,c,yr,s,size,a,d){const x=((t*s*d)%(w+size*2))-size;const y=h*yr+Math.sin(t*s*.002)*30;const g=ctx.createRadialGradient(x,y,0,x,y,size);g.addColorStop(0,rgba(c,a));g.addColorStop(.35,rgba(c,a*.35));g.addColorStop(1,rgba(c,0));ctx.save();ctx.globalCompositeOperation='screen';ctx.fillStyle=g;ctx.beginPath();ctx.arc(x,y,size,0,Math.PI*2);ctx.fill();ctx.restore()}
function draw(ms){const t=ms*CONFIG.speed;drawBackground();drawFloatingGlow(ms,COLORS[1],.28,.06,320,.06,1);drawFloatingGlow(ms+1200,COLORS[3],.68,.04,360,.05,-1);drawFloatingGlow(ms+2100,COLORS[2],.50,.05,280,.045,1);for(const l of layers)drawWaveLayer(l,t);requestAnimationFrame(draw)}
window.addEventListener('resize',resize);resize();requestAnimationFrame(draw);
